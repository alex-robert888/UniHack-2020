
import './BaseAuthCard.scss';

const BaseAuthCard = ({baseInputType, baseInputIcon, baseInputTitle, baseInputArguments}) => {
    return (
        <form className='base-card'>
            
        </form>
    );
}

export default BaseAuthCard;